const links = {
  hyperLink: {
    textDecoration: 'underline',
    ':hover,:focus,active': {
      color: 'primary',
    },
  },
  footer: {
    ':hover,:focus,.active': {
      color: 'primary',
    },
  },
};

const buttons = {
  primary: {},
  secondary: {},
};

const cards = {
  primary: {},
};

export const elements = {
  buttons,
  links,
  cards,
};
